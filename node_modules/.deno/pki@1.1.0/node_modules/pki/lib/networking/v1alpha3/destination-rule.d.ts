import { CustomResourceOptions } from '@pulumi/pulumi';
import * as k8s from '@pulumi/kubernetes';
import { input } from '../../types';
export declare class DestinationRule extends k8s.apiextensions.CustomResource {
    constructor(name: string, args: input.networking.v1alpha3.DestinationRuleArgs, opts?: CustomResourceOptions);
}
