import { CustomResourceOptions } from '@pulumi/pulumi';
import * as k8s from '@pulumi/kubernetes';
import { input } from '../../types';
export declare class ServiceEntry extends k8s.apiextensions.CustomResource {
    constructor(name: string, args: input.networking.v1alpha3.ServiceEntryArgs, opts?: CustomResourceOptions);
}
