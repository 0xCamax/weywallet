import { CustomResourceOptions } from '@pulumi/pulumi';
import * as k8s from '@pulumi/kubernetes';
import { input } from '../../types';
export declare class HTTPAPISpecBinding extends k8s.apiextensions.CustomResource {
    constructor(name: string, args: input.mixer.v1.HTTPAPISpecBindingArgs, opts?: CustomResourceOptions);
}
